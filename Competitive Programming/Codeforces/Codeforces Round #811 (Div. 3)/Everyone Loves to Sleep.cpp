#include<bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        int n, H, M, h, m;
        cin >> n >> H >> M;
        for(int i = 0; i < n; i++)
        {
            cin >> h >> M;
            if(h >= H)
            {
                cal_h = h - H;
                cal = m
            }

        }

    }
    return 0;
}
