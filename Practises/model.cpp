#include "bits/stdc++.h"
using namespace std;

int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        char c;
        cin >> c;
        if(c == 'c' || c == 'o' || c == 'd' || c == 'e' || c == 'f' || c == 'r' || c == 'e' || c == 's')
            cout << "yes" << "\n";
        else
            cout << "no" << "\n";

    }

    return 0;
}
